// innerText
const p = document.querySelector("p")
console.log(p.innerText);
// textContent
console.log(p.textContent);
// innerHTML
console.log(p.innerHTML);

const header = doc ument.querySelector("h1")
header.innerText = "TextChanged"
header.innerHTML = "<del>TextChanged</del>"
