// classlist
// add()
// remove()
// toggle()

const h1 = document.querySelector("h1")
console.log(h1.classList); // allows to check all the classes attached to the element
 
h1.classList.add("styles") //adds class "styles" to h1

h1.classList.remove("styles")// removes class

h1.classList.toggle('styles')// toggles